import styled from '@emotion/styled';

import { ButtonPrimary } from 'ui/common/components/Buttons/ButtonPrimary/ButtonPrimary';

export const NavigatorButton = styled(ButtonPrimary)`
  margin-left: auto;
  margin-top: 20px;
`;
